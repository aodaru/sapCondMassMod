[tools]
python = "3.8"

[tasks.setup]
description = "Preparacion de las herramientas a utilizar"
run = """
    pip install pywin32 pandas dotenv PySide2
    python -m pip install --upgrade pip
"""   

[settings]
python.github_attestations = false